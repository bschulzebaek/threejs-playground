import SceneContainer from '@/components/SceneContainer';

export default function CarShowroom() {
    return (
        <div>
            <SceneContainer name={'CarShowroomDescriptor'} />
        </div>
    )
}